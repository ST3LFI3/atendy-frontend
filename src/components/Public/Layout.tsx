import CardLocale from "./CardLocale";

export default function Layout(){
    return <div>
        <CardLocale name={'O lenhador 1'} key={'1'}/>
        <CardLocale name={'O lenhador 2'} key={'2'} />
    </div>
}